//this is the js simple validator exercise

//My first attempt//
/* var phone = prompt('What is your phone number?');
var userInput = prompt();
    console.log(phone.charAt(4, 8) === '-' && (userInput.length ===10));
return alert('You have entered a correct phone number')
*/

/* 
//This returns whether it is the user entry is tru or not
var phone = prompt('Please enter your phone number with dashes:');
alert(phone.charAt(3) === '-' && phone.charAt(7) === "-" && phone.length === 12);
*/

/*
// this returns whether a date of birth was entered correctly
var dob = prompt('Please enter your date of birth in this form xx/xx/xxxx');
alert(dob.charAt(2) === '/' && dob.charAt(5) === '/' && dob.length === 10);
*/

/* 
//This returns whether a zip code entered is true or false 
var postalCode = prompt('Please enter your zip code');
 alert(postalCode.length === 5 || postalCode.charAt(5) === '-' && postalCode.length === 10);
*/

/* 
// this returns whether a state is entered in correct format
var state = prompt('What state do you live in? Please abbreviate ie:MN')
alert(state.length === 2 && state === state.toUpperCase());
*/

/*
//this returns an answer to whether you are married and calls the function .toLowerCase no matter how user puts it into the form
var married = prompt("Are you married? Please answer yes or no");
alert(married.toLowerCase() === 'yes' || alert(married.toLowerCase() === 'no'));
*/

                     


